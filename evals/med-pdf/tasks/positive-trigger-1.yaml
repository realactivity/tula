id: positive-trigger-001
name: Lab PDF (text-extractable)
description: |
  User mentions a LabCorp panel PDF. Skill should recognize lab-parsing trigger
  and describe extract/parse workflow.
tags:
  - routing-positive
  - positive-trigger
  - labs
  - text-extractable
inputs:
  prompt: |
    I just downloaded my latest LabCorp panel as a PDF. Can you take a look
    and tell me what's worth paying attention to compared to my last set?
expected:
  outcomes:
    - type: task_completed
graders:
  - type: text
    name: recognizes_lab_workflow
    config:
      regex_match:
        - "(?i)(extract|parse|lab|panel|biomarker|abnormal|compare|trend)"
